/******* Do not edit this file *******
Simple Custom CSS and JS - by Silkypress.com
Saved: May 16 2025 | 11:54:54 */
// window.ignoreEvent = false;
document.addEventListener('DOMContentLoaded', function () {
  const isDarkMode = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
// console.log(isDarkMode);

  if (isDarkMode) {
	  console.log('enter in dk mode');
	  	setTimeout(function() {
     document.querySelector('.dracula-toggle.mode-light').click();
			console.log('click on dk mode');
	  }, 600); 
  }else{
	  console.log('enter in light mode');
	  setTimeout(function() {
	   document.querySelector('.dracula-toggle.mode-dark').click();
		  console.log('click on light mode');
	  }, 600); 
  }

  // Listen for changes in system theme
  window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', e => {
    const darkModeOn = e.matches;
// 	  console.log(darkModeOn);
    if (darkModeOn) {
      document.querySelector('.--dark').click();
    } else {
      document.querySelector('.--light').click();
    }
  });
});

